
**Document Status:** 🧩 Draft  
**Version:** 0.2.0  
**Maintainer:** Nuengtoppin  
**Last updated:** 2025-11-26  
**Purpose:** Conceptual overview of the Arden Engine design and topology  

---

# 🧠 **Arden Engine — Concept Overview**

*(гибридный воксельно-векторный движок, R&D-архитектура)*  

Arden Engine — экспериментальный open-source движок,  
объединяющий **воксельные**, **векторные**, **топологические** и **динамические** принципы  
в единой математической модели.  

Главная цель — создать стабильное ядро, способное одинаково хорошо работать с:

- разрушаемыми мирами,  
- процедурной генерацией,  
- динамическими объектами,  
- гибридом «воксель + меш»,  
- оптимизациями под LOD / SVO.  

---

## **Стратегия и принципы**

**Arden Engine** строится вокруг идеи *единства представления данных*:  
всё — от региона до вокселя, от контейнера до полигона —  
должно подчиняться одной логике адресации, симметрии и преобразований.

Основные принципы:
1. **Мир как иерархия контейнеров.** Всё описывается топологически, без “особых случаев”.  
2. **Адрес как универсальный идентификатор.** Route = абсолютная координата + путь.  
3. **Ротация как симметрия.** Любой объект может быть переориентирован без потери связности.  
4. **Контейнер как живая единица.** DUN — основа для динамических структур.  
5. **Единый язык данных.** Воксель и вектор работают в одной математической системе.  
6. **EQ-Kernel как баланс.** Мир должен быть согласованным, а не просто вычисляемым.  

---

## **Системный уровень**

### **1. Иерархическая топология**

```

Region → Block → Chunk → Octochunk → Voxel

```

Каждый уровень имеет фиксированный размер и строгие отношения вложенности.  
Вся структура обратима и пригодна для Morton-индексации.  
Это даёт однозначное кодирование пространства и идеальное соответствие ECS-модели.

Идея — сделать универсальные страйды для всего движка, а не придумывать множество разных.  
Считаю это правильным решением, чем изобретать на ходу костыли и хаки под каждую реализацию,  
так как наслышан о неудобствах и неоправданных сложностях,  
где даже десять специалистов будут ломать голову, как это реализовать.

### **2. Routing Layer (адресация)**

Route = путь точки в мире.  
Он обеспечивает двунаправленное преобразование **Route ↔ XYZ**,  
служит основой для всех подсистем — от генерации до физики.  

Фактически это “протокол адресов” движка.  
Не путать с другими тезисами — это большой блок адресации,  
где для ядра и других модулей применяется унифицированный формат поиска и взаимодействия.

Подробнее опишу в отдельном блоке:  
[Routing Specification](./Routing_Spec.md)  

Для инженеров уже есть готовый техпаспорт.

### **3. Rotation / Orientation Layer**

Дискретные повороты (0°, 90°, 180°, 270°) позволяют выполнять ротацию контейнеров и объектов  
без потери топологии — ключ к модульным структурам и копированию областей.

---

### **4. DUN-Containers (Dynamic Unit Nodes)**

Динамические контейнеры — переносные “локальные миры” внутри основного.  
Они содержат собственную топологию, могут спать, вращаться, архивироваться.  
DUN — фундамент для всей динамики и симуляции в гибридном ядре.

Тема широкая и требует глубокого подхода.  
Как база, она уже есть в папке архитектуры,  
но для расширения блока стоит протестировать:  
уже есть первое ТЗ для демонстрации, соблюдены версии для MVP.  
После проверки теории и API появится больше понимания.

---

### **5. Two-Module Hybrid Core**

Движок разделён на два взаимосвязанных слоя:

**Voxel Module:** хранение, разрушение, SVO, физика, генерация.  
**Vector Module:** триангуляция, упрощение, гибридный рендер.

Оба слоя говорят на одном языке — **Route / Topology** —  
поэтому данные переходят между ними без конфликтов.  

Мир не будет чисто воксельным, но это не значит, что нельзя использовать воксели как основу.  
Если двигаться дальше в реализации, есть два пути:  
либо рендерить воксели как есть — вариант допустимый, но мир останется кубическим,  
и мы быстро упрёмся в логический потолок;  
либо реализовать триангуляцию.  

В теории, если мы сгладим меш из кубических вокселей в грубую топологическую векторную сетку,  
то сможем использовать этот слой как подслой для поддержания целостности мира  
(практика применима: помогает не проваливаться под землю из-за микротрещин),  
а поверх него — дизайнерский слой (финальный меш).  

Всё это опционально для обсуждения,  
но я бы смотрел в эту сторону, чем ограничиваться воксельным мешем.

---

### **6. EQ-Kernel (Equilibrium Kernel)**

Сердце движка — система равновесия.  
EQ управляет активностью, следит за согласованностью,  
координирует взаимодействие модулей и готовит данные для симуляции.

Пока размышлял, как это ядро в целом может связывать и DUN, и единую адресацию.  
Идея в том, что ядро — “истина мира”: оно существует до рендера, независимо от графики,  
при этом разделено на два слоя:

#### Внутренние слои EQ:
- **EQ-Core** — баланс и целостность данных.  
- **EQ-Sim** — симуляция физики и разрушений.  

Более подробно опишу в отдельном блоке:  
[EQ-Kernel Overview](./EQ_Kernel.md)

---

### **7. HAOS (Hybrid Adaptive Optimization System)**

Адаптивный слой оптимизаций между ядром и шиной.  
Следит за LOD, активностью регионов, приоритетами рендера, балансом нагрузки.  
Работает над ECS как динамический балансировщик.

---

### **8. BUS-Layer**

Внутренняя шина между EQ / HAOS и GPU.  
Группирует потоки, собирает батчи, управляет приоритетами вывода.  
Его задача скорее напоминает работу почтальона —  
он только расставляет очередь на рендер, при этом не знает, что именно распределяет.  

BUS не принимает решений — он лишь передаёт пакеты данных,  
соблюдая порядок и приоритет, определённые системой.  
Порядок очереди задаёт разработчик,  
а оптимизация шины — трудоёмкий процесс для команды.  

Он крайне необходим для любых больших систем:  
без такого слоя всегда будут появляться фризы, глюки, баги, вылеты.  
А если его нет или он “размазан” по системе — отладка становится ещё сложнее.  
Поэтому он должен быть строго описан и поставлен на своём месте.

Более подробно опишу в отдельном блоке.

---

### **9. Render Pipeline (WGPU)**

Объединяет меши и воксели, работает с SSAO / PFO,  
выполняет батчинг и постобработку.  
Задача — визуализировать мир, не ломая его дискретную структуру.  

На ранних этапах используется WGPU, который, как я понял, отлично ложится под будущие API —  
переход на Vulkan и подобные.  
Мы не ограничены в развитии, просто на старте писать сразу под Vulkan слишком сложно,  
особенно на раннем этапе.  

Перед выводом кадров применяется **PFO-фильтр**.  

В двух словах, **PFO** — это косметический слой,  
где сосредоточена “вся красота”: он динамически упрощает рендер  
(например, не рендерит то, чего не видно).  
Тут же — шейдеры, постэффекты, частицы, текстуры.  
Логичнее держать это здесь, а не размазывать по всему движку.  
Пока поверхностно — материал будет дорабатываться и дописываться.  

Блок дополню отдельно:  
[Render & PFO Layer](./Render_Pipeline.md)

---

### **10. Песочница и MVP**

Первая цель — минимальный прототип мира:  
debug-панели, инспекция топологии, тесты контейнеров и ротации,  
маленькое 2.5D-демо с живыми системами DUN и EQ.  

После демонстрации с MVP 0.1 и далее (чистая сцена на стабильной версии Bevy и доп. библиотеках).  
Выбрал Bevy 0.13 не зря — для таких сцен он более стабильный и не конфликтует с egui и другими.  

Для реализации полноценного сырого движка нужно минимум,  
чтобы можно было прокатить идею в том же 2.5D-варианте.  
Так проще понять направление и вовремя остановиться,  
а не бежать в массовую разработку.  

Поэтому 2.5D-движок с готовыми “лопатами” — уже отдельная фаза.  
Это не просто прототип движка, а платформа для первых игровых экспериментов.  
Хоть он и на 20–30% от полной реализации,  
этот шаг уже доказал, что проект не впустую — это стратегически важный опыт.  

Более подробно опишу в отдельном блоке.

---

## **Итог**

Раздел **Concept** служит исходной точкой для:
- проектирования архитектуры (`/ARCHITECTURE`),
- подготовки MVP (`/MVP`),
- обновления терминов (`/TERMS`).  

Блок будет дорабатываться и разбиваться на отдельные части для удобства.  

---

📚 [Вернуться к дорожной карте →](../roadmap.md)

---
